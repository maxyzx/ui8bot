<%= form_with(model: product_file, local: true) do |form| %>
  <% if product_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_file.errors.count, "error") %> prohibited this product_file from being saved:</h2>

      <ul>
      <% product_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :_id %>
    <%= form.text_field :_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
